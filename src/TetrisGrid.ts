import {Grid} from "./Grid";

export class TetrisGrid extends Grid {

    loseAnim() {
        document.getElementById("")
        let row = this.getHeight() -1;
        let col = 0;
        const interval = setInterval(() => {

        }, 33.33);
    }
}